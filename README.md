# Personal Portfolio

中文个人作品集网站。基于 Next.js App Router、TypeScript 与原生 CSS 构建，默认配置为静态导出。

## 本地开发

```bash
npm install
npm run dev
```

## 质量检查

```bash
npm run lint
npm run typecheck
npm run build
npm test
```

生产构建输出位于 `out/`。

完整页面地图、数据替换清单和部署说明见 `IMPLEMENTATION.md`。
